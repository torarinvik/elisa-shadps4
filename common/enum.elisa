# SPDX-FileCopyrightText: Copyright 2024 shadPS4 Emulator Project
# SPDX-License-Identifier: GPL-2.0-or-later

struct Flags[T]:
    bits: mutable u64

def Flags_new[T](value: u64 = 0) -> Flags[T]:
    return Flags[T](value)

def Flags_set[T](self: mutable Flags[T]&, bit: u64) -> void:
    self.bits <- self.bits | bit

def Flags_clear[T](self: mutable Flags[T]&, bit: u64) -> void:
    self.bits <- self.bits & ~bit

def Flags_has[T](self: Flags[T]&, bit: u64) -> bool:
    return (self.bits & bit) != 0

def Flags_value[T](self: Flags[T]&) -> u64:
    return self.bits

