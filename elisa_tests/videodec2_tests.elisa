# SPDX-FileCopyrightText: Copyright 2024-2026 shadPS4 Emulator Project
# SPDX-License-Identifier: GPL-2.0-or-later

include "../core/libraries/videodec/videodec2.elisa"
include "../../compiler/runtime/elisacore_std/elisacore_runtime.elisa"

@test
def videodec2_structs_roundtrip() -> void:
    can Abort.Panic:
        cfg: mutable OrbisVideodec2DecoderConfigInfo = zeroed
        cfg.thisSize <- 0x48
        cfg.maxFrameWidth <- 1920
        cfg.optimizeProgressiveVideo <- true
        assert cfg.thisSize == 0x48
        assert cfg.maxFrameWidth == 1920
        assert cfg.optimizeProgressiveVideo == true
        assert cfg.maxFrameHeight == 0

        mem: mutable OrbisVideodec2DecoderMemoryInfo = zeroed
        mem.cpuMemorySize <- 4096
        mem.frameBufferAlignment <- 256
        assert mem.cpuMemorySize == 4096
        assert mem.frameBufferAlignment == 256

        inp: mutable OrbisVideodec2InputData = zeroed
        inp.auSize <- 1024
        assert inp.auSize == 1024

        out: mutable OrbisVideodec2OutputInfo = zeroed
        out.isValid <- true
        out.pictureCount <- 2
        out.frameWidth <- 1280
        assert out.isValid == true
        assert out.pictureCount == 2
        assert out.frameWidth == 1280

        fb: mutable OrbisVideodec2FrameBuffer = zeroed
        fb.frameBufferSize <- 8192
        fb.isAccepted <- true
        assert fb.frameBufferSize == 8192
        assert fb.isAccepted == true

        cmem: mutable OrbisVideodec2ComputeMemoryInfo = zeroed
        cmem.cpuGpuMemorySize <- 512
        assert cmem.cpuGpuMemorySize == 512

        ccfg: mutable OrbisVideodec2ComputeConfigInfo = zeroed
        ccfg.computePipeId <- 3
        ccfg.computeQueueId <- 5
        ccfg.checkMemoryType <- true
        assert ccfg.computePipeId == 3
        assert ccfg.computeQueueId == 5
        assert ccfg.checkMemoryType == true
