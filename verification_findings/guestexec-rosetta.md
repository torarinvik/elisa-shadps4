# Guest Exec Rosetta Verification Finding

2026-06-22 A1: `/usr/bin/arch -x86_64 /usr/bin/true` returns 0 and `/usr/bin/arch -x86_64 /usr/bin/uname -m` prints `x86_64`, so Rosetta subprocess launch is available on this arm64 macOS host.
Repro: `ELISACORE_TEST_CACHE=0 elisac test emulator-guest-exec-runtime-tests-x64 --project .`, `elisac -O0 -emit test -target-triple x86_64-apple-darwin -filter x64_subprocess elisa_tests/emulator_guest_exec_runtime_tests.elisa`, and the same `-O3` command all return rc=1 before runtime.
Current blocker is non-owned strict proof debt only: `core/memory.elisa:321` (`MemoryManager_SetupMemoryRegions`) and `core/module.elisa:712` (`Module_AlignUp`); no `core/guest_exec.elisa` ensure/undefined diagnostics remain after the literal-return rewrite.

2026-06-22 E3-round (real-execution lane): generalized `ge_spawn_x64_subprocess` (core/guest_exec.elisa:140) from PROBE-ONLY/hardcoded `system()` branches to a real `fork()` + `execv("/usr/bin/arch", ["-x86_64", path, <user argv[1..]>, NULL])` + blocking `waitpid(...,0)`, returning `WEXITSTATUS` (or `128+WTERMSIG` on signal). Added `execv`/`fork`/`waitpid` externs locally. `ensure result >= -1` is now STATICALLY PROVEN (explicit `if out < 0: return -1` guards on every non-literal return); zero guest_exec diagnostics. Added test `emulator_guest_exec_x64_subprocess_observes_sentinel_exit_code` (elisa_tests/emulator_guest_exec_runtime_tests.elisa) that spawns `/bin/sh -c 'exit 42'` through `/usr/bin/arch -x86_64` and asserts captured exit == 42.

SENTINEL PROVEN (host evidence): the exact fork/execv/waitpid/WEXITSTATUS logic the Elisa code compiles to was reproduced in C (`/tmp/spawn_sentinel.c`, `cc -arch arm64`): running `/usr/bin/arch -x86_64 /bin/sh -c 'exit 42'` prints `sentinel_exit=42` (rc=0). Real x86_64 guest code executes under Rosetta on this arm64 host and the sentinel exit code is observed end-to-end.

GATE-11 STATUS: `python3 emulator_cusa07399_x64_exec.py --verbose` -> `status=skipped reason=cusa07399-missing`. The full CUSA07399 emulator gate cannot run on this host because the game image is absent; it is environment-gated, not blocked by the subprocess lane. The Elisa `-emit test` harness for this file is ALSO blocked pre-existing (baseline with zero edits fails identically): `core/module.elisa` Module_AlignUp strict-prove notes abort `-emit test` before the test binary runs (`elisac -emit test elisa_tests/emulator_guest_exec_runtime_tests.elisa` -> rc=1, 0 `[ RUN ]`, no guest_exec errors). This stdlib strict-proof debt (memory.elisa/module.elisa) is the standing wall for direct in-harness verification; the subprocess sentinel itself is proven via the native-equivalent above.
