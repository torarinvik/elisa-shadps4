# PENDING project.json target stanza — E1 first-frame capture

Add to `project.json` test targets (do NOT let me edit project.json directly; this is the
stanza another agent should merge):

```json
{
  "name": "emulator-renderer-first-frame-capture",
  "kind": "elisacore-test",
  "entry": "elisa_tests/emulator_renderer_first_frame_capture.elisa"
}
```

Verified standalone with:
`ELISACORE_TEST_CACHE=0 elisac -emit test elisa_tests/emulator_renderer_first_frame_capture.elisa`
=> passed=1.

---

## AlignUp no-overflow `requires` (Module_AlignUp / KernelMemory_AlignUp)

No project.json change required for this work — it edits existing source
(`core/module.elisa`, `core/linker.elisa`, `core/libraries/kernel/memory.elisa`)
only. Recorded here per the constraint to not touch project.json / check_elisa_port.py.

Status: source-side guards landed; end-to-end discharge is BLOCKED on the paired
compiler change C1 (requires→ensure threading + guard-fact→call-site-precondition
flow). The installed `elisac` does NOT yet honor the new `requires` (verified: the
counterexample `alignment=2, value=2^64-1` is still reported even though
`requires value+alignment <= 0xFFFFFFFFFFFFFFFF` excludes it, both with and without
`-smt`). See verification_findings/alignup-01.md for full detail.

---

## shader-bitfield-protocol-tests

Add to `project.json` test targets (do NOT let me edit project.json directly; this is the
stanza another agent should merge):

```json
{
  "name": "shader-bitfield-protocol-tests",
  "kind": "elisacore-test",
  "entry": "elisa_tests/shader_bitfield_protocol_tests.elisa"
}
```

Verified standalone with:
`ELISACORE_TEST_CACHE=0 elisac -emit test elisa_tests/shader_bitfield_protocol_tests.elisa`
=> passed=4.
