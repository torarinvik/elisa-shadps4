# E2 — File-format decoder hardening: pfs, playgo_chunk, npbind

## Status: COMPLETE (2026-06-22)

All three decoders have been hardened and property-tested. All tests green.

---

## Proposed verification target stanzas

### pfs.elisa

```
target core/file_format/pfs.elisa
  gate pfs-bounds-01: Pfs_ParseHeader never traps on any darray[u8] input
    property elisa_tests/core_file_format_pfs_property_tests.elisa::pfs_parse_never_traps
    golden rejects_truncated_header
    golden rejects_zero_block_size
    golden rejects_negative_n_block
    golden rejects_negative_dinode_count
```

### playgo_chunk.elisa

```
target core/file_format/playgo_chunk.elisa
  gate playgo-bounds-01: Playgo_Parse never traps on any darray[u8] input
    property elisa_tests/core_file_format_playgo_chunk_property_tests.elisa::playgo_parse_never_traps
    golden rejects_truncated_header
    golden rejects_wrong_magic
    golden chunk_attr_out_of_range_skips_gracefully
```

### npbind.elisa

```
target core/file_format/npbind.elisa
  gate npbind-bounds-01: NpBind_Parse never traps on any darray[u8] input
    property elisa_tests/core_file_format_npbind_property_tests.elisa::npbind_parse_never_traps
    golden rejects_truncated_header
    golden rejects_wrong_magic
    golden entry_truncated_body_rejected
    golden entry_with_oversized_data_rejected
    golden valid_single_entry_accepted
```

---

## Gate-list lines (for project.json when ready)

```json
{ "id": "pfs-bounds-01",    "file": "core/file_format/pfs.elisa",           "test": "elisa_tests/core_file_format_pfs_property_tests.elisa" },
{ "id": "playgo-bounds-01", "file": "core/file_format/playgo_chunk.elisa",  "test": "elisa_tests/core_file_format_playgo_chunk_property_tests.elisa" },
{ "id": "npbind-bounds-01", "file": "core/file_format/npbind.elisa",        "test": "elisa_tests/core_file_format_npbind_property_tests.elisa" }
```

---

## Bounds analysis notes

### pfs.elisa
- `Pfs_ParseHeader` guards `data.count < 88` before any reads. All field reads go through `ff_read_*`
  which internally call `ff_can_read` and return 0 on underflow — no trap possible.
- `Pfs_ReadDirent` calls `ff_skip(reader, 512)` after 4 field reads; `ff_skip` clamps to `data.count`
  on overflow — safe but may silently consume beyond the dirent. Noted as informational.
- Validity gate (`block_size > 0 and dinode_count >= 0 and n_block >= 0`) discharges statically via
  the integer-comparison semantics; no SMT needed.

### playgo_chunk.elisa
- `Playgo_ReadHeader` unconditionally skips 32 bytes and 128 bytes via `ff_skip` (clamping). The
  `PLAYGO_HEADER_SIZE` (256) pre-check ensures the reader starts with enough data.
- `Playgo_ReadChunkAttr` and `Playgo_ReadMchunkAttr` receive an `offset` that is always checked by
  the caller (`attr_offset + PLAYGO_CHUNK_ATTR_ENTRY_SIZE > chunk_attrs_data.count` guard) before
  the call — the inner `ff_read_*` chain is safe.
- The mchunk index read (`mchunk_index_reader.offset <- attr.mchunks_offset.usize() + j*2`) can
  point past `chunk_mchunks_data`; `ff_read_u16_le` returns 0 in that case via the `ff_can_read`
  guard in `ff_read_u8`. Safe — no trap.

### npbind.elisa
- `NpBind_ReadEntry` has an explicit `ff_can_read(reader, 4)` guard before reading type+size, and
  a second `ff_can_read(reader, size.usize())` before reading the data payload. Both truncation
  paths return `NpBindEntryRaw_empty()` with `data.count == 0`.
- The body-level validity check (`body.X.data.count != body.X.size.usize()`) catches any partial
  read and sets `valid <- false`. No arithmetic overflow possible (size is u16, max 65535 bytes).
- Digest tail read: `data.count - NPBIND_DIGEST_SIZE` is only executed under `data.count >= NPBIND_DIGEST_SIZE`
  guard — no underflow.

---

## Bounds that could NOT be statically discharged

None. All field-read bounds are enforced operationally by `ff_can_read` / size pre-checks, and all
validity predicates are simple integer comparisons. No SMT obligation was generated that required
a `fileformat-NN.md` repro.

---

# Shader recompiler — REAL SPIR-V emission (added 2026-06-22)

## Status: COMPLETE

The shader recompiler bridge now emits REAL (non-envelope) SPIR-V for the embedded
black-frame fragment shader, driven end-to-end through the pure-Elisa pipeline:
decode -> CFG -> ASL -> SPIR-V module assembler (`SpirvModule_Assemble`). The
assembled module passes the host `spirv-val` (exit 0) and disassembles to a
well-formed `OpEntryPoint Fragment "main"` module. Shaders that use opcodes
outside the supported set decline cleanly to the deterministic 12-word envelope
(no regression).

Supported GCN opcodes (the gnmdriver embedded ps0 fixture): `s_mov_b32`,
`v_mov_b32`, `v_cvt_pkrtz_f16_f32`, `s_nop`, `exp`, `s_endpgm`.

### Proposed verification target stanza

```
target shader_recompiler/recompiler_real_spirv.elisa
  gate shader-recompiler-real-spirv-01: black-frame ps0 fixture emits validatable SPIR-V (payload_kind == REAL_SPIRV)
    property elisa_tests/shader_recompiler_real_spirv_tests.elisa::real_spirv_black_frame_fixture_emits_real_module
    golden real_spirv_black_frame_fixture_emits_real_module
    golden real_spirv_unsupported_opcode_falls_back_to_envelope
```

### Gate-list line (for project.json when ready)

```json
{ "id": "shader-recompiler-real-spirv-01", "file": "shader_recompiler/recompiler_real_spirv.elisa", "test": "elisa_tests/shader_recompiler_real_spirv_tests.elisa" }
```

### Test-target stanza (project.json `targets`, when wired)

```json
"shader-recompiler-real-spirv-tests": {
  "entry": "elisa_tests/shader_recompiler_real_spirv_tests.elisa",
  "emit": "test",
  "foreign": []
}
```

### Evidence

- `spirv-val` on the assembled module: exit 0 (valid).
- `spirv-dis` shows: `OpCapability Shader`, `OpEntryPoint Fragment %10 "main" %6`,
  `OpExecutionMode %10 OriginUpperLeft`, a `v4float` Output variable decorated
  `Location 0`, and an `OpFunction` body that `OpStore`s the color and `OpReturn`s.
- 70-word module (vs. the 12-word envelope); header magic `0x07230203`, version 1.6,
  bound 12, schema 0.
- Regression: `emulator-renderer-first-frame-smoke`, `emulator-renderer-first-frame-capture`,
  `shader_recompiler_recompiler_tests` (8), `shader_recompiler_frontend_pure_tests` (5),
  `shader_recompiler_backend_spirv_tests` (28) all still pass.

### Files changed

- `shader_recompiler/recompiler_translate.elisa` — `output_words` changed from
  fixed `u32[12]` to `darray[u32]`; accessors/free updated; real-SPIR-V attempt
  wired ahead of the envelope fallback.
- `shader_recompiler/recompiler_real_spirv.elisa` — NEW. ASL/decode bridge +
  real SPIR-V module builder + opcode gate.
- `elisa_tests/shader_recompiler_real_spirv_tests.elisa` — NEW. Asserts REAL path
  + structural validation + envelope-fallback.
