# E4 — SaveData PS4-ABI Completeness + Oracle

## Status
COMPLETE — all 6 tests passing (2026-06-22).

## Target stanza

```
"savedata-param-oracle-tests": {
  "entry": "elisa_tests/savedata_param_oracle_tests.elisa",
  "inherit-project-native": false,
  "foreign": [],
  "emit": "test"
}
```

Gate line (add to CI / check script):
```
ELISACORE_TEST_CACHE=0 elisac -emit test elisa_tests/savedata_param_oracle_tests.elisa
```

## What was done

### 1. Completed OrbisSaveDataParam field decode (`savedata_abi.elisa`)

Added missing constants and helpers:
- `SD_PARAM_OFF_MTIME = 1288` (was absent; comment said s64 but no offset const)
- `SD_PARAM_SIZE = 1296` (total struct size for bounds documentation)
- `abi_s64_at(v, off) -> s64` — reads 8 bytes as little-endian signed 64-bit
- `abi_store_s64(v, off, value)` — writes 8 bytes as little-endian s64

Extended `savedata_abi_set_param`:
- `SD_PARAM_DETAIL` (type=3): routes to `savedata_set_param_detail`
- `SD_PARAM_MTIME`  (type=5): reads 8-byte s64 from guest buf → `savedata_set_param_mtime`

Extended `savedata_abi_get_param`:
- `SD_PARAM_DETAIL` (type=3): copies detail string + writes got_size
- `SD_PARAM_MTIME`  (type=5): writes 8-byte s64 → guest buf + got_size=8

Extended `savedata_abi_dir_name_search` param fill loop:
- now emits `detail@256` and `mtime@1288` per hit alongside existing title and user_param

### 2. Native mount core (`save_mount.elisa`)

`SaveMount` struct: added `mtime: mutable s64` and `detail: mutable static u8&?`

`SaveSearchHit` struct: added `mtime: mutable s64` and `detail: mutable static u8&`

`savedata_flush_sfo`: persists DETAIL (text) + MTIME_LO/MTIME_HI (integer pair) to param.sfo

`savedata_load_sfo`: restores detail and reconstructs s64 mtime from MTIME_LO/MTIME_HI

`savedata_search`: loads DETAIL and MTIME from each param.sfo hit

`savedata_mount`: initialises `mtime <- 0` and `detail <- null` in the new slot

New functions:
- `savedata_set_param_detail(handle, detail: u8&?) -> u32`
- `savedata_get_param_detail(handle, out_detail: mutable u8&, cap, out_got_size) -> u32`
- `savedata_set_param_mtime(handle, mtime: s64) -> u32`
- `savedata_get_param_mtime(handle, out_mtime: mutable s64&, out_got_size) -> u32`

### 3. Oracle test file (`elisa_tests/savedata_param_oracle_tests.elisa`)

Tests (6 total, all green):

| Test | Kind | Covers |
|------|------|--------|
| `param_oracle_full_roundtrip_property` | `@property` | encode all 5 fields → decode → identity |
| `param_oracle_golden_vector_offsets` | `@test` | exact byte offsets + NUL termination + zero-padding check |
| `param_oracle_mtime_little_endian` | `@test` | s64 byte order golden vector |
| `param_oracle_mtime_negative_roundtrip` | `@test` | -1 encodes as 0xFF*8 |
| `param_oracle_live_detail_mtime_roundtrip` | `@test` | set/get + umount/remount SFO persistence |
| `param_oracle_abi_set_get_detail_mtime` | `@test` | ABI entry-point path (sceSaveDataSetParam/GetParam type=3/5) |

## Files modified

- `core/libraries/save_data/savedata_abi.elisa` — constants, helpers, set/get param branches, search fill
- `core/libraries/save_data/save_mount.elisa` — struct fields, sfo flush/load, new param functions
- `elisa_tests/savedata_param_oracle_tests.elisa` — new oracle test file (created)
